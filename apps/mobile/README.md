# apps/mobile — Flutter mobile app

Not yet scaffolded. Planned entry point for Phase 0:

```
flutter create --org com.welfareplatform --platforms=android,ios .
```

Member-facing app: registration, contributions, claims, notifications, member wallet (Phase 2). Separate Dart/pub ecosystem — not part of the root npm workspace, but versioned in this repo alongside `api` and `admin`.
