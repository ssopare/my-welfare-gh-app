# Local dev only. The admin app is the *only* thing that ever calls the
# NestJS API directly — server-to-server, never from the browser — so this
# is a plain server-side env var, never NEXT_PUBLIC_. See the admin UI
# design plan's "Auth & API integration architecture" section for why.
API_URL="http://localhost:3000"
